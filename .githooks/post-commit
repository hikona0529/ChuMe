#!/bin/sh
branch="$(git branch --show-current)"

if [ -z "$branch" ]; then
  echo "No current branch, skip auto push."
  exit 0
fi

if git rev-parse --abbrev-ref "@{upstream}" >/dev/null 2>&1; then
  git push
else
  git push -u origin "$branch"
fi